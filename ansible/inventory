---
boxes:
  hosts:
    streambox:
      # configure these targets on the router prior to running
      ansible_host: streambox.home
      ansible_user: stream-admin

vm:
  hosts:
    # Hosts are VMs on the streambox
    # IP addresses will be statically assigned
    obs-server:
      ansible_host: 192.168.122.101
      ansible_user: stream-admin
      ansible_ssh_common_args: -o ProxyCommand="ssh -p 22 -W %h:%p stream-admin@streambox.home"